export default async function() {
    // console.log(Object.keys(prisma));
    // const allBalances = await prisma.user.findMany();
    // console.log(allBalances);
    return <div>
        Dashboard Page (or transfer/txn page)
    </div>
}